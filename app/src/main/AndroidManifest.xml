<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:name=".StudyMateApplication"
        android:allowBackup="true"
        android:label="StudyMate"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyMate">
        <meta-data android:name="firebase_messaging_installation_id_enabled" android:value="true" />
        <service
            android:name=".StudyMateMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <activity android:name=".ChatActivity" />
        <activity android:name=".ConversationsActivity" />
        <activity android:name=".PartnerDetailsActivity" />
        <activity android:name=".SearchPartnersActivity" />
        <activity android:name=".StudyRequestDetailsActivity" />
        <activity android:name=".MyRequestsActivity" />
        <activity android:name=".CreateRequestActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
